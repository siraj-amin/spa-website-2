import Link from 'next/link'
import { blogs } from '@/lib/blogs'
export default function BlogPage(){return <main className="page-main"><section className="page-hero"><div className="wrap"><p className="eyebrow">Journal</p><h1>Stories for a softer pace.</h1><p>Thoughtful notes on rest, care, privacy and wellbeing.</p></div></section><section className="wrap blog-list"><div className="blog-grid">{blogs.map(blog=><Link className="blog-card" href={`/blog/${blog.slug}/`} key={blog.slug}><div className="blog-card-image"><img src={blog.image} alt="" /></div><div className="blog-card-body"><span className="eyebrow">{blog.category} · {blog.readTime}</span><h2>{blog.title}</h2><p>{blog.excerpt}</p><span className="text-link">Continue reading →</span></div></Link>)}</div></section></main>}
