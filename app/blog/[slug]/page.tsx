import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { blogs, getBlog } from '@/lib/blogs'
export function generateStaticParams(){return blogs.map(blog=>({slug:blog.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const blog=getBlog(slug);if(!blog)return {};return {title:`${blog.title} | Karachi Best Escorts`,description:blog.excerpt,alternates:{canonical:`/blog/${slug}/`},openGraph:{type:'article',title:blog.title,description:blog.excerpt,images:[blog.image]}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const blog=getBlog(slug);if(!blog)notFound();return <main className="page-main"><section className="article-hero"><div className="wrap"><p className="breadcrumb"><Link href="/blog/">Blogs</Link> / {blog.title}</p><span className="eyebrow">{blog.category} · {blog.readTime}</span><h1>{blog.title}</h1><p>{blog.excerpt}</p></div></section><article className="wrap article-content"><div className="article-main"><div className="article-hero-image"><img src={blog.image} alt={blog.title}/></div><div className="article-body">{blog.body.map((paragraph,i)=><section key={paragraph}><h2>{i===0?'A considered approach':i===1?'Begin with a simple intention':'Make space for recovery'}</h2><p>{paragraph}</p>{i===1&&<h3>Create a ritual that fits your day</h3>}</section>)}<Link className="service-link" href="/blog/">Back to all Blogs →</Link></div></div><aside><h2>Suggested Blogs</h2><div className="suggested-grid">{blogs.filter(item=>item.slug!==slug).slice(0,3).map(item=><Link className="blog-card" href={`/blog/${item.slug}/`} key={item.slug}><div className="blog-card-image"><img src={item.image} alt=""/></div><div className="blog-card-body"><h3>{item.title}</h3><p>{item.excerpt}</p><span className="text-link">Continue reading →</span></div></Link>)}</div></aside></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({'@context':'https://schema.org','@type':'Article',headline:blog.title,description:blog.excerpt,author:{'@type':'Organization',name:'Karachi Best Escorts'},image:[blog.image],datePublished:'2026-01-01'})}}/></main>}
